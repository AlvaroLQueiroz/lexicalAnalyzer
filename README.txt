Para compilar abra um terminal, navegue até a pasta Analisador lexico e execute o arquivo "Makefile" com o comando "make".

Para executar abra um terminal, navegue até a pasta Analisador lexico e execute o arquivo "Lexico" com o comando "./Lexico <caminho do arquivo>",
substituindo o "<caminho do arquivo>" pelo caminho exato do arquivo.
